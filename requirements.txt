# Orders service dependencies
#
# Dependency refresh:
#   aiohttp      3.7.4   -> 3.9.0   (newer, but still carries known CVEs)
#   cryptography 2.3     -> 41.0.0  (newer, but still carries known CVEs)
#   urllib3      1.24.1  -> 2.7.0   (latest, no known CVEs)
aiohttp==3.9.0
cryptography==41.0.0
urllib3==2.7.0
